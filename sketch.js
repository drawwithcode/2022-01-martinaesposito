function preload() {
}

function setup() {
  createCanvas(windowWidth, windowHeight);

  noFill();
  strokeCap(SQUARE);
  stroke(30);
  strokeWeight(10);

}

function draw() {

  let arcWeight = 10;
  let dia = 100;
  let angleModule = PI / 8;



}
